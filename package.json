{
  "name": "seven",
  "version": "1.0.0",
  "description": "Fixed for render deploy",
  "scripts": {
    "dev": "vite",
    "build": "vite build && esbuild server.js --bundle --platform=node --format=esm --outfile=dist/server.js",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "vite": "^5.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "express": "^4.18.2",
    "axios": "^1.4.0",
    "lucide-react": "^0.258.0",
    "@vitejs/plugin-react": "^4.0.0",
    "esbuild": "^0.19.2"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "typescript": "^5.3.3"
  }
}